{
  "name": "tiktok-battle-royale-3d",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Desktop-first 3D TikTok battle royale with a lightweight Hugging Face deployable backend.",
  "workspaces": [
    "backend",
    "desktop",
    "shared"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel backend:dev desktop:dev",
    "backend:dev": "npm --workspace backend run dev",
    "backend:start": "npm --workspace backend start",
    "desktop:dev": "npm --workspace desktop run tauri:dev",
    "desktop:build": "npm --workspace desktop run tauri:build",
    "build": "npm --workspace shared run build && npm --workspace desktop run build",
    "check": "npm --workspace backend run check && npm --workspace desktop run check",
    "test": "npm --workspace backend test"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
